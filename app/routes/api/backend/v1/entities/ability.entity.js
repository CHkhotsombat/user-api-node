export const abilityDetail = (ability) => {
  const {
    id,
    name,
    code,
    createdAt,
    updatedAt,
  } = ability

  return {
    id,
    name,
    code,
    createdAt,
    updatedAt,
  }
}
